/**
 * Re-export from ai.ts for backward compatibility
 */
export {
  generateFlashcardsForModule,
  detectProvider,
  getProviderDisplayName,
  extractAndParseJson,
  SupportedProvider,
} from './ai';
